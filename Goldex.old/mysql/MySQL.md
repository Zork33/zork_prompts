Чтобы поднять MySQL в Docker-контейнере и импортировать в него `.sql`-бэкап, можно сделать это в два быстрых шага:

---

### 🔧 Шаг 1: Запустите контейнер MySQL

```bash
docker run --name mysql-backup -e MYSQL_ROOT_PASSWORD=your_root_password -e MYSQL_DATABASE=mydb -d -p 3306:3306 mysql:8.0
```


```bash
docker run --name mysql-goldex -e MYSQL_ROOT_PASSWORD=ktulhu44 -e MYSQL_DATABASE=goldex -d -p 3306:3306 mysql:8.0
```


Пояснение:
- `--name mysql-backup` — имя контейнера (удобно для последующих команд)
- `-e MYSQL_ROOT_PASSWORD=...` — задаёт пароль root (обязательно!)
- `-e MYSQL_DATABASE=mydb` — (опционально) создаёт базу при старте
- `-p 3306:3306` — проброс порта на хост (если нужно подключаться извне)
- `mysql:8.0` — версия образа (можно использовать `latest`, но лучше фиксировать)

> 💡 Убедитесь, что файл `backup.sql` находится в доступной директории на хосте (например, `./backup.sql`).

---

### 📥 Шаг 2: Импортируйте SQL-бэкап

#### Вариант A: Импорт через `docker exec` (если контейнер уже запущен)

```bash
docker cp ./backup.sql mysql-backup:/backup.sql
docker exec -i mysql-backup mysql -uroot -pyour_root_password mydb < /backup.sql
```

Или одной командой без копирования (работает в Linux/macOS):

```bash
docker exec -i mysql-backup mysql -uroot -pyour_root_password mydb < ./backup.sql
```

```bash
docker exec -i mysql-goldex mysql -uroot -pktulhu44 goldex < ./backup_tm-dev_2025-11-23T16-33-00.sql 
```

> ⚠️ На Windows в PowerShell `<` может не работать — используйте `Get-Content`:

```powershell
Get-Content backup.sql | docker exec -i mysql-backup mysql -uroot -pyour_root_password mydb
```

#### Вариант B: Автоматический импорт при старте (через volume)

Поместите `.sql`-файл в папку, например `./init/`, и смонтируйте её в `/docker-entrypoint-initdb.d`:

```bash
docker run --name mysql-backup \
  -e MYSQL_ROOT_PASSWORD=your_root_password \
  -e MYSQL_DATABASE=mydb \
  -v $(pwd)/init:/docker-entrypoint-initdb.d \
  -d -p 3306:3306 mysql:8.0
```

Все `.sql` (и `.sh`) файлы из этой директории автоматически выполнятся **при первой инициализации** контейнера.

> ⚠️ Это работает **только если том с данными пуст** (т.е. первый запуск). Если контейнер уже существовал — удалите его и том перед повтором.

---

### ✅ Проверка

Подключитесь и убедитесь, что данные импортированы:

```bash
docker exec -it mysql-backup mysql -uroot -pyour_root_password mydb -e "SHOW TABLES;"
```

---

Если нужно — могу помочь адаптировать под ваш конкретный бэкап или сценарий (например, импорт в существующую БД, кодировку, пользователей и т.п.).