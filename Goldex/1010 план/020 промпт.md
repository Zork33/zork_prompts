  Выполняй шаги из C:\work\goldex\business\docs\plan3.md
  Для каждого шага:
  1) сделай необходимые изменения в коде/доках;
  2) покажи, что именно сделано (файлы/пути);
  3) при необходимости запускай команды (с запросом на эскалацию, если нужно);
  4) после каждого шага — короткий отчет.
  В конце — сделай детально документированный коммит и пуш.

  Если нужно — добавь конкретику по окружению/ограничениям, например:

  Используй Docker для тестового Postgres. Не вноси changes вне docs/ и packages/db/.

  Или:

  Если нужны данные/секреты (DATABASE_URL и т.п.), остановись и спроси.

  Такой промпт гарантирует исполнение, правки и команды, а не только план/описание.