Ок, сделаю и шаблоны, и git-адаптацию 👇

---

## 1. Шаблоны файлов (под copy‑paste)

### 1.1. `project.md` — описание проекта

```markdown
# Проект: <Название проекта>

## Краткое описание
<1–3 предложения, что это и зачем>

## Цели проекта
- [ ] Цель 1
- [ ] Цель 2
- [ ] Цель 3

## Критерии успеха
- <Как поймём, что результат хороший?>
- <Например: есть согласованное ТЗ; покрыты такие-то кейсы; получен прототип и т.п.>

## Ограничения и предположения
- Технологии: ...
- Сроки: ...
- Команда / роли: ...
- Прочее: ...

## Исходные материалы
- Ссылка 1: ...
- Ссылка 2: ...
- Файл: ...
```

---

### 1.2. `plan.md` — план и статус

```markdown
# План по проекту

## Текущее состояние
- Активная ветка: A_main
- Последний шаг: A_002 (детализация ролей)
- Общий статус: в работе / заморожен / завершён

## Крупные этапы
1. Сформулировать и согласовать ТЗ — ветка A_main
2. Разработать архитектуру — ветка B_*
3. Спецификация реализации / дизайн — ветка C_*
4. Финализация документов — ветка D_*

## Задачи по этапам

### Этап 1. ТЗ
- [x] A_001 — собрать первичные требования
- [x] A_002 — уточнить роли и права (частично, есть проблемы)
- [ ] A_003 — доработать раздел логирования и мониторинга

### Этап 2. Архитектура
- [ ] B_001 — первый вариант архитектуры
- [ ] B_002 — альтернативная архитектура

## История изменений плана (кратко)
- 2025-12-01: создан проект, заведена ветка A_main
- 2025-12-02: по результатам A_002 создана ветка B_alt-from-A_002
```

---

### 1.3. `journal.md` — журнал шагов

```markdown
# Журнал работы с ИИ

## Шаблон записи

## <Дата> <Время> | <Ветка> / <Шаг-ID>

- Цель шага:
- Файлы:
  - prompt: ...
  - result_raw: ...
- Статус: ✅ / ⚠️ / ❌
- Краткий результат:
- Проблемы:
- Решение / следующее действие:

---

## 2025-12-01 15:32 | A_main / A_001

- Цель шага: собрать первичное ТЗ по системе
- Файлы:
  - prompt: branches/A_main/runs/A_001/prompt.md
  - result_raw: branches/A_main/runs/A_001/result_raw.md
- Статус: ✅ Успешно
- Краткий результат: получен черновик ТЗ с разделами: цели, роли, основные сценарии
- Проблемы: нет
- Решение / следующее действие:
  - A_002 — уточнить роли и права, добавить логирование

---

## 2025-12-01 18:05 | A_main / A_002

- Цель шага: детализировать роли и права
- Файлы:
  - prompt: branches/A_main/runs/A_002/prompt.md
  - result_raw: branches/A_main/runs/A_002/result_raw.md
- Статус: ⚠️ Частично
- Краткий результат: роли описаны, но администратор и модератор перепутаны
- Проблемы:
  - Неверное разделение ответственности между ролями
- Решение / следующее действие:
  - Создать ветку B_alt-from-A_002 с более жёстким промптом и примерами
```

---

### 1.4. `branch-info.md` — паспорт ветки

#### Для основной ветки

```markdown
# Ветка A_main

## Общая информация
- Идентификатор ветки: A_main
- Родительская ветка: нет (основная)
- Точка ответвления: n/a
- Цель ветки:
  - Получить основное согласованное ТЗ по проекту

## Правила работы в ветке
- Не править `result_raw.md` (только читать)
- Все изменения и корректировки делать через новые промпты и новые шаги (`runs/A_00X`)
- Если нужно “изменить направление” — создаём новую ветку, а не переписываем существующие шаги

## История шагов (кратко)
- A_001 — черновик ТЗ — ✅ успешно
- A_002 — детализация ролей — ⚠️ частично, есть проблемы с разделением полномочий

## Итоговый статус ветки
- Статус: в работе / завершена / тупиковая / заморожена
- Итоговый артефакт: (путь к файлу, если ветка завершена)
- Комментарий: (почему ветка остановлена/завершена)
```

#### Для ответвившейся ветки

```markdown
# Ветка B_alt-from-A_002

## Общая информация
- Идентификатор ветки: B_alt-from-A_002
- Родительская ветка: A_main
- Точка ответвления: шаг A_002
- Причина создания:
  - Исправить ошибки в ролях (администратор vs модератор), не ломая основную ветку A_main

## Гипотеза / стратегия
- Чётко перечислить роли и их обязанности в промпте
- Добавить примеры ситуаций, чтобы ИИ развёл обязанности корректно
- Ограничить свободу творчества (“не вводить новые роли без явного запроса”)

## Правила работы в ветке
- Не менять файлы в ветке A_main
- Ветки считают родительские результаты “историей”, но не переписывают их

## История шагов (кратко)
- B_001 — переработка ролей с явными примерами — (статус после выполнения)
```

---

### 1.5. `prompt.md` — шаблон промпта для шага

```markdown
# Метаданные шага (НЕ отправлять ИИ, это для нас)
- Проект: <Название проекта>
- Ветка: <A_main / B_alt-from-A_002 / ...>
- Шаг: <A_001 / B_001 / ...>
- Цель шага (одним предложением): <что хотим от ИИ>

---

# Системные инструкции (можно сокращать при отправке)
Ты — <роль ИИ: архитектор, аналитик, разработчик и т.п.>.
Отвечай структурировано и по делу.
Если чего-то не хватает, задавай уточняющие вопросы в начале.

# Контекст проекта
<Кратко: 5–15 строк, выжимка из project.md / предыдущих результатов.>

Пример:
- Проект: система управления заявками
- Цель этапа: формализовать роли и права пользователей
- Уже есть: черновик ТЗ с общими сценариями (см. выдержку ниже)

## Важные выдержки из предыдущих шагов
<Сюда вставляем только нужные куски из прошлых result_raw.md / итоговых документов.>

---

# Задача для ИИ на этот шаг

Сформулируй / переработай / дополни:
<конкретная задача, максимально чётко>

Обязательные требования:
1. ...
2. ...
3. ...

---

# Формат ответа

Выведи результат в таком формате:

1. Краткое резюме (3–5 пунктов)
2. Основная часть:
   - ...
3. Список открытых вопросов (если есть)
```

> Живой приём: всё, что выше линии `---` с “Метаданные шага”, **можно не посылать ИИ**, это чисто ваш внутренний шаблон.

---

### 1.6. `evaluation.md` — оценка шага

```markdown
# Оценка шага <A_001>

## Общая информация
- Ветка: A_main
- Шаг: A_001
- Дата: 2025-12-01
- Статус: ✅ Успешно / ⚠️ Частично / ❌ Неудачно

## Что удалось хорошо
- ...
- ...

## Проблемы и недостатки
- ...
- ...

## Вывод
<1–3 предложения: годится ли результат и для чего>

## Следующие действия
- [ ] Создать шаг A_002: <краткое описание>
- [ ] Или создать новую ветку: <имя>, если меняем стратегию

## Заметки
<любые дополнительные комментарии, которые могут пригодиться в будущем>
```

---

## 2. Адаптация этой схемы под Git

Теперь — как всё это подружить с git, чтобы был нормальный откат, diff и история.

### 2.1. Базовая идея

- Вся папка `ai/` — **один git-репозиторий**.
    
- Структуру `project/branches/...` оставляем такой же, как описано выше.
    
- **Git-ветки не обязаны совпадать** с ветками A_main / B_... — это логические ветки внутри проекта.
    
- Git используем:
    
    - для истории изменений файлов;
        
    - для откатов;
        
    - для тегов “версия спецификации v1”, “финальное ТЗ v2” и т.п.
        

---

### 2.2. Старт

В корне `ai/`:

```bash
git init
echo ".DS_Store" >> .gitignore        # если на macOS
echo "tmp/" >> .gitignore             # если будут временные файлы
git add .
git commit -m "init: базовая структура для проектов с ИИ"
```

---

### 2.3. Типичный цикл работы с ИИ + git

1. **Готовим новый шаг**
    
    - Создаём папку, например `branches/A_main/runs/A_003/`.
        
    - Заполняем `prompt.md`.
        
2. **Запускаем ИИ**
    
    - Копируем ответ в `result_raw.md`.
        
    - Заполняем `evaluation.md`.
        
    - Обновляем `journal.md` и `plan.md`.
        
3. **Фиксируем в git**
    

```bash
git add branches/A_main/runs/A_003 \
        journal.md \
        plan.md

git commit -m "A_003: доработал логирование и мониторинг (частично успешно)"
```

4. **При необходимости** — ставим тег на “важную” точку:
    

```bash
git tag -a v0.1-spec -m "Первая версия чернового ТЗ"
```

Так у тебя:

- в файловой структуре — понятные ветки/шаги;
    
- в git — история и “снапшоты” с тегами.
    

---

### 2.4. Откат через git

Есть два уровня отката:

1. **Логический откат по нашей схеме**
    
    - В `journal.md` находим нужный шаг (например, A_001).
        
    - Считаем, что “актуальным” является тот результат (`result_raw.md`).
        
    - От него делаем новую ветку (B_*) и новый шаг — _без удаления того, что было дальше_.
        
2. **Технический откат через git**  
    Когда нужно вернуть конкретный файл в состояние на определённый коммит:
    

```bash
# Посмотреть историю файла
git log --oneline -- branches/A_main/runs/A_002/prompt.md

# Вернуть версию файла из старого коммита (не делая глобальный reset)
git checkout <commit_hash> -- branches/A_main/runs/A_002/prompt.md
```

После этого — обычный `git add` + `git commit`, и у тебя появляется новый коммит с “откаченной” версией файла.

---

### 2.5. Использовать ли git-ветки для A_main / B_…?

**Рекомендация по умолчанию:**  
Не городить 1:1 связь между `A_main` и git-веткой. Пусть:

- логические ветки (A_main, B_alt-from-...) — это **папки** в `branches/`;
    
- git-ветка чаще всего одна — `main`.
    

Так проще:

- видны все ветки сразу в одном рабочем каталоге;
    
- можно поискать по всему проекту (`rg`, `grep`, поиск по IDE);
    
- не нужно прыгать между git-ветками.
    

**Когда имеет смысл git-ветки:**

Например, если у одного и того же проекта есть два _конкурирующих_ больших направления (монолит vs микросервисы), и ты хочешь держать их раздельно:

- git branch `monolith` — в нём папка `ai/2025-12-01_project/branches/...` со своим набором A/B.
    
- git branch `microservices` — аналогично, но с другими ветками и шагами.
    

Но это уже “уровень бог” и обычно не нужен.

---

### 2.6. Как git помогает с “не правим result_raw через ИИ”

В твоей логике важно:

> при проблемах мы не исправляем через ИИ результат, а меняем промпт и делаем новую ветку.

Git тут — страховка:

- `result_raw.md` **никогда не редактируем** (по договорённости).
    
- Если случайно поправили — всегда можно посмотреть diff и вернуть файл в исходный вид:
    

```bash
git diff branches/A_main/runs/A_002/result_raw.md
git checkout -- branches/A_main/runs/A_002/result_raw.md
```

---

Если хочешь, дальше могу:

- помочь тебе собрать “скелет” реального проекта (с уже разложенными файлами и заполненными заголовками),
    
- или вместе с тобой придумать конкретные имена веток/шагов и первые 2–3 записи в `journal.md` под твою реальную задачу.